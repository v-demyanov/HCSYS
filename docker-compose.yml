version: '3.4'

name: hcsys
services:
  api:
    image: ${API_IMAGE_PATH}:${API_TAG}
    build:
      context: .
      dockerfile: Services/Patients/HCSYS.PatientsService.Api/Dockerfile

networks:
  default:
    name: hcsys
